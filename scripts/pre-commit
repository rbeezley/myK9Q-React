#!/bin/sh
# Pre-commit hook for myK9Q React
# Runs type checking before allowing commit

echo "🔍 Running type check..."

npm run typecheck

if [ $? -ne 0 ]; then
  echo "❌ Type check failed. Please fix TypeScript errors before committing."
  exit 1
fi

echo "✅ Type check passed!"
exit 0
